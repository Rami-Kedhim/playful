
// Re-export components from the auth folder
import { useAuth, AuthProvider } from './useAuth';
import { useRole } from './useRole';

export { useAuth, AuthProvider, useRole };
export default useAuth;
