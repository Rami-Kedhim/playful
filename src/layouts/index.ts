
import MainLayout from './MainLayout';
import UnifiedLayout from './UnifiedLayout';

export { MainLayout, UnifiedLayout };

// Export a default Layout that extends MainLayout for easier imports
export const Layout = MainLayout;
